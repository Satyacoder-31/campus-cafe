{
  "name": "campus-cafe",
  "version": "1.0.0",
  "description": "Campus Café — College Canteen Management System",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "setup:db": "cd server && npx prisma generate && npx prisma migrate dev --name init && node prisma/seed.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start": "echo 'Run dev:server and dev:client in separate terminals. See README.md'"
  },
  "keywords": ["canteen", "food-ordering", "mern", "campus"]
}
